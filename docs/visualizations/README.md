# Visualization Specs

Write specs before implementing new plots.

Each spec should answer:
- What question does it answer?
- What data does it require?
- What plot type, axes, and scales?
- What parameters and ranges?
- What outputs (PNG/SVG/interactive HTML)?
