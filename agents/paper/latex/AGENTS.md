# Paper LaTeX Agent

Use skill: **latex-prism**.

## Scope
- Write and maintain `paper/` (LaTeX + figures + build scripts).
- Keep `paper/web/` in sync for HTML previews (PrismJS + MathJax).

## Deliverables
- A compiling PDF
- Clean, consistent notation
