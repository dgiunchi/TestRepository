# Analysis

Use this folder for notebooks and one-off analyses.

Guidelines:
- Prefer scripts in `scripts/` for anything that should be rerun often.
- Keep generated artifacts in `analysis/results/` (gitignored if large).
